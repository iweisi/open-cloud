<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.github.lyd</groupId>
    <artifactId>open-cloud</artifactId>
    <packaging>pom</packaging>
    <version>0.2.0</version>
    <description>开放云平台</description>
    <modules>
        <module>docs/generator</module>
        <module>opencloud-common</module>
        <module>opencloud-gateway</module>
        <module>opencloud-upms</module>
        <module>opencloud-acms</module>
        <module>opencloud-msg</module>
        <module>opencloud-zipkin</module>
    </modules>
    <!-- 环境 -->
    <profiles>
        <!-- 开发 -->
        <profile>
            <id>dev</id>
            <activation>
                <!--默认激活配置-->
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <profile.name>dev</profile.name>
                <!--配置中心地址-->
                <config.server-addr>127.0.0.1:8848</config.server-addr>
                <!--服务发现地址-->
                <discovery.server-addr>127.0.0.1:8848</discovery.server-addr>
                <!--认证授权地址-->
                <auth.server-addr>http://localhost:8211</auth.server-addr>
                <!--网关服务地址-->
                <gateway.server-addr>http://localhost:8888</gateway.server-addr>
            </properties>
        </profile>
        <!-- 测试 -->
        <profile>
            <id>test</id>
            <activation>
                <activeByDefault>false</activeByDefault>
            </activation>
            <properties>
                <profile.name>test</profile.name>
                <!--配置中心地址-->
                <config.server-addr>127.0.0.1:8848</config.server-addr>
                <!--服务发现地址-->
                <discovery.server-addr>127.0.0.1:8848</discovery.server-addr>
                <!--认证授权地址-->
                <auth.server-addr>http://localhost:8211</auth.server-addr>
                <!--网关服务地址-->
                <gateway.server-addr>http://localhost:8888</gateway.server-addr>
            </properties>
        </profile>
        <!-- 生产 -->
        <profile>
            <id>online</id>
            <activation>
                <activeByDefault>false</activeByDefault>
            </activation>
            <properties>
                <profile.name>online</profile.name>
                <!--配置中心地址-->
                <config.server-addr>127.0.0.1:8848</config.server-addr>
                <!--服务发现地址-->
                <discovery.server-addr>127.0.0.1:8848</discovery.server-addr>
                <!--认证授权地址-->
                <auth.server-addr>http://localhost:8211</auth.server-addr>
                <!--网关服务地址-->
                <gateway.server-addr>http://localhost:8888</gateway.server-addr>
            </properties>
        </profile>
    </profiles>
    <properties>
        <java.version>1.8</java.version>
        <!-- 文件拷贝时的编码 -->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <maven.compiler.encoding>UTF-8</maven.compiler.encoding>
        <maven-compiler-plugin.version>3.6.2</maven-compiler-plugin.version>
        <maven-resources-plugin.version>3.1.0</maven-resources-plugin.version>
        <maven-source-plugin.version>3.0.1</maven-source-plugin.version>
        <maven-surefire-plugin.version>2.22.0</maven-surefire-plugin.version>

        <spring-boot.version>2.0.4.RELEASE</spring-boot.version>
        <spring-cloud.version>Finchley.RELEASE</spring-cloud.version>

        <alibaba.nacos.version>0.2.0.RELEASE</alibaba.nacos.version>
        <alibaba.fastjson.version>1.2.37</alibaba.fastjson.version>
        <alibaba.druid.version>1.1.9</alibaba.druid.version>

        <mysql.version>5.1.45</mysql.version>
        <tk.mybatis.version>2.0.4</tk.mybatis.version>
        <github.pagehelper.version>1.2.10</github.pagehelper.version>

        <apache.commons-lang3.version>3.3.2</apache.commons-lang3.version>
        <apache.commons-io.version>2.5</apache.commons-io.version>
        <apache.commons.beanutils>1.9.3</apache.commons.beanutils>
        <google.guava.version>19.0</google.guava.version>
        <httpclient.version>4.5.2</httpclient.version>
        <swagger2.version>2.7.0</swagger2.version>

        <!--自定义版本-->
        <opencloud.common.version>0.2.0-SNAPSHOT</opencloud.common.version>
        <opencloud.rbac-client.version>0.2.0-SNAPSHOT</opencloud.rbac-client.version>
        <opencloud.auth-client.version>0.2.0-SNAPSHOT</opencloud.auth-client.version>
        <opencloud.gateway-client.version>0.2.0-SNAPSHOT</opencloud.gateway-client.version>
        <opencloud.acms-client.version>0.2.0-SNAPSHOT</opencloud.acms-client.version>
        <opencloud.msg-client.version>0.2.0-SNAPSHOT</opencloud.msg-client.version>
    </properties>
    <!--公共依赖-->
    <dependencies>
        <!--监控-->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <!--测试依赖-->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>
        <!--自定义配置处理器,配置项提示-->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
    </dependencies>
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
                <version>${alibaba.nacos.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-starter-alibaba-nacos-config</artifactId>
                <version>${alibaba.nacos.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <resources>
            <!-- 先指定 src/main/resources下所有文件及文件夹为资源文件 -->
            <resource>
                <directory>src/main/resources</directory>
                <includes>
                    <include>**/*</include>
                </includes>
                <filtering>true</filtering>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven-compiler-plugin.version}</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <encoding>${maven.compiler.encoding}</encoding>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <version>${maven-resources-plugin.version}</version>
            </plugin>
            <plugin>
                <!--打包跳过测试-->
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven-surefire-plugin.version}</version>
                <configuration>
                    <skipTests>true</skipTests>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>${maven-source-plugin.version}</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>